sudo: false

language: node_js

node_js:
  - '6'
  - '7'
  - '8'

cache:
  yarn: true
  directories:
    - node_modules

script: "npm test"
after_script:
  - cat ./coverage/lcov.info | ./node_modules/codecov/bin/codecov
